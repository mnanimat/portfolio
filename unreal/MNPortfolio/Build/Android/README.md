# Android

Esta pasta está reservada para ícones, splash e arquivos Gradle/Manifest específicos do projeto.

O scaffold não instala nem copia SDK, NDK ou JDK. Configure o Turnkey da Unreal Engine 5.8 e confirme `ANDROID_HOME` antes de executar `Scripts/Package-Android.ps1`.
